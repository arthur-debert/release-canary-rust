# Consumer override for release-core init. Adds the opt-in `bats`
# Component (ships bin/check-e2e + the harness) on top of the rust
# Stack defaults — same shape as padz, the reference adopter.
capabilities:
  - rust-quality
  - bats
